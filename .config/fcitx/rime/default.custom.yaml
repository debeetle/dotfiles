# Rime default settings
# encoding: utf-8

config_version: '0.40'

patch: 
  schema_list:
    - schema: flypy

  switcher:
    caption: 〔方案〕
    hotkeys:
      - F4

  menu/page_size: 3

  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Shift_L: commit_text
      Shift_R: noop
      Control_L: noop
      Control_R: noop
      Caps_Lock: clear
      Eisu_toggle: clear
